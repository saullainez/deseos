import { FiltroCmpletadoPipe } from './filtro-cmpletado.pipe';

describe('FiltroCmpletadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCmpletadoPipe();
    expect(pipe).toBeTruthy();
  });
});
